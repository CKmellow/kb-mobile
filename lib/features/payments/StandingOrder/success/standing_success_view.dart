import 'package:flutter/material.dart';

class StandingSuccessView extends StatelessWidget {
  const StandingSuccessView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Placeholder for the standing order success page
    return Scaffold(
      appBar: AppBar(title: const Text('Standing Order Created')),
      body: const Center(child: Text('Standing Order Created Successfully!')),
    );
  }
}
